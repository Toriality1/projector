// ============================================================================
// src/types.ts
// ============================================================================

export interface ProjectorOptions {
  ignore: string[];
  include?: string;
  output?: string;
  dryRun?: boolean;
  toClipboard?: boolean;
  includeHidden?: boolean;
}
